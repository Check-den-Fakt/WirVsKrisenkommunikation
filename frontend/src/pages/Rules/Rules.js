import React, { useState } from 'react';



export default function Rules() {
  // Declare a new state variable, which we'll call "count"
  return (
    <div>
      <p>Die 10 Goldenen Regeln</p>
    </div>
  );
}